1. Instale los requerimientos necesarios en un entorno virtual si lo desea.
    pip install virtualenv
    virtualenv (nombre)

2. Instale los requerimientos necesarios.
    pip install -r requirements.txt

3. Utilize la consulta SQL en su base de datos.
    archivo database.txt

4. Dentro del archivo config.py dentro de la carpeta 'app', configure los espacios vacios con los datos necesarios.

5. Ejecute el archivo run.py
    python \run.py